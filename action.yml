name: R6 Redirect Fixture
description: Owned harmless action for redirect policy validation
runs:
  using: composite
  steps:
    - shell: bash
      run: echo "R6_ACTION_MARKER=POLICY_DISALLOWED_TARGET_EXECUTED"
